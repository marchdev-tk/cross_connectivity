name: cross_connectivity_example
description: Demonstrates how to use the cross_connectivity package.

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cross_connectivity:
    path: ../

flutter:
  uses-material-design: true
